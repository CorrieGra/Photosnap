/**
 * 
 * @param { number } num the number that you want you want check for being even
 * @returns { boolean } return true or false based on if the number is even or not
 */
export const isEven = (num) => num % 2 === 0;